spirng:
  profiles:
    active: test

api:
  file-server:
    url: http://${host.file-server}
  api-server:
    url: http://${host.api-server}

host:
  file-server: localhost:${wiremock.server.port}
  api-server: localhost:${wiremock.server.port}
